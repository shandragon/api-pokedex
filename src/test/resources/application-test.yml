spring:
  datasource:
    url: jdbc:h2:mem:testdb;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password:

  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create-drop
    defer-datasource-initialization: false

  sql:
    init:
      mode: never

  data:
    mongodb:
      port: 0

app:
  seguranca:
    token-admin: token-teste
    jwt-secret: Y2hhdmUtand0LXBhcmEtdGVzdGVzLWRlLWRlc2Vudm9sdmltZW50by0yNTZi
    token-ttl-horas: 1
    bloqueio-tentativas: 5
    bloqueio-duracao-minutos: 15

de:
  flapdoodle:
    mongodb:
      embedded:
        version: 7.0.0
