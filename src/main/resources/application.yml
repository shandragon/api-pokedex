spring:
  profiles:
    active: dev
  datasource:
    url: jdbc:h2:mem:pokedexdb;DB_CLOSE_DELAY=-1
    driverClassName: org.h2.Driver
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
  h2:
    console:
      enabled: true
